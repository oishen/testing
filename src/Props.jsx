
const Props = ({name, brand, color}) => {

    // const {name, brand, color} = p;
  return (
    <div>
        <h2>{name}</h2>
        <p>{brand}</p>
        <p> {color} </p>
    </div>
  )
}

export default Props